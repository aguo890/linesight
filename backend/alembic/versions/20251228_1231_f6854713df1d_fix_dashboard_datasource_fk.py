"""fix_dashboard_datasource_fk

Revision ID: f6854713df1d
Revises: a1b2c3d4e5f6
Create Date: 2025-12-28 12:31:02.832481+00:00

"""

import contextlib
from collections.abc import Sequence

from alembic import op

# revision identifiers, used by Alembic.
revision: str = "f6854713df1d"
down_revision: str | None = "a1b2c3d4e5f6"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    # Drop the old foreign key constraint pointing to excel_uploads
    # If it fails (e.g. already dropped), just proceed
    with contextlib.suppress(Exception):
        # Using the actual name confirmed from MySQL logs/SHOW CREATE
        op.drop_constraint("dashboards_ibfk_2", "dashboards", type_="foreignkey")

    # Create the new foreign key constraint pointing to data_sources
    op.create_foreign_key(
        "dashboards_datasource_fk",
        "dashboards",
        "data_sources",
        ["data_source_id"],
        ["id"],
        ondelete="SET NULL",
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_constraint("dashboards_datasource_fk", "dashboards", type_="foreignkey")
    op.create_foreign_key(
        "dashboards_ibfk_2",
        "dashboards",
        "excel_uploads",
        ["data_source_id"],
        ["id"],
        ondelete="SET NULL",
    )
    # ### end Alembic commands ###
